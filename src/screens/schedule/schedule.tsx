import { SafeScreen } from '@/components';
import { ScheduleScreen } from './scheduleScreen';

export const Schedule = () => {
  return (
    <SafeScreen>
      <ScheduleScreen />
    </SafeScreen>
  );
};
