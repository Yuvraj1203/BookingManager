import { SafeScreen } from '@/components';
import { SettingScreen } from './settingScreen';

export const Settings = () => {
  return (
    <SafeScreen>
      <SettingScreen />
    </SafeScreen>
  );
};
