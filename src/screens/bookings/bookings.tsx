import { SafeScreen } from '@/components';
import { BookingScreen } from './bookingScreen';

export const Bookings = () => {
  return (
    <SafeScreen>
      <BookingScreen />
    </SafeScreen>
  );
};
